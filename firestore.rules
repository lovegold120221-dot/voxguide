rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is not used by this application - deny all access
    // Data is stored in Supabase (PostgreSQL) instead
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
